<%= form_for(cartola) do |f| %>
  <% if cartola.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cartola.errors.count, "error") %> prohibited this cartola from being saved:</h2>

      <ul>
      <% cartola.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
